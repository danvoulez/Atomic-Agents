# Rust Machinery

Crates:
- `logline`: parse/serialize LogLine blocks.
- `tdln-in`: compile natural language into LogLine spans.
- `tdln-out`: render JSON back to natural language.
- `napi-bindings`: exposes the machinery to Node via NAPI.
