{
  "name": "@ai-coding-team/worker",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "DATABASE_URL=postgres://postgres:devpassword@localhost:55432/ai_coding_team vitest run",
    "test:l0": "DATABASE_URL=postgres://postgres:devpassword@localhost:55432/ai_coding_team LLM_PROVIDER=mock vitest run tests/l0",
    "test:l2": "DATABASE_URL=postgres://postgres:devpassword@localhost:55432/ai_coding_team vitest run tests/l2",
    "test:github": "vitest run tests/github-auth.test.ts tests/github-integration.test.ts",
    "test:repo-provider": "vitest run tests/repo-provider.test.ts",
    "test:integration": "vitest run tests/github-auth.test.ts tests/github-integration.test.ts tests/repo-provider.test.ts"
  },
  "dependencies": {
    "@ai-coding-team/types": "1.0.0",
    "@ai-coding-team/agents": "1.0.0",
    "@ai-coding-team/db": "1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "express": "^4.18.2",
    "js-yaml": "^4.1.0",
    "typescript": "^5.3.0",
    "vitest": "^1.6.0"
  }
}
